if [ -d $HOME/.bin/script ]; then
        export PATH=$HOME/.bin/script:$PATH
fi

if [ -f $HOME/.bin/bash_aliases ]; then
        . $HOME/.bin/bash_aliases
fi

if [ -f $HOME/.bin/bash_completion ]; then
        . $HOME/.bin/bash_completion
fi
